import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    // paddingHorizontal: 6,
    // paddingVertical: 2
    minHeight: 20,
    minWidth: 20
  },
  content: {
    flex: 1,
    alignSelf: 'stretch',
    justifyContent: 'center',
    alignItems: 'center',
  }
});